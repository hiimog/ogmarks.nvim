{
  "name": "ogmarks.nvim",
  "version": "1.0.0",
  "description": "todo",
  "main": "dist/index.js",
  "repository": "https://github.com/hiimog/ogmarks.nvim.git",
  "author": "OG <og@hiimog.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "clean": "yarn rimraf dist",
    "rel:build": "yarn tsc -p ./config/tsconfig.rel.json",
    "test": "yarn clean && yarn test:build && yarn test:run",
    "test:run": "yarn jest -c ./dist/config/jest.config.js",
    "test:build": "yarn tsc -p ./config/tsconfig.test.json"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^18.14.2",
    "jest": "^29.4.3",
    "neovim": "^4.10.1",
    "rimraf": "^4.1.3",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "ts-node": "^10.9.1"
  }
}
