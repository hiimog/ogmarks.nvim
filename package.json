{
  "name": "ogmarks.nvim",
  "version": "1.0.0",
  "description": "todo",
  "main": "index.js",
  "repository": "https://github.com/hiimog/ogmarks.nvim.git",
  "author": "OG <og@hiimog.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "clean": "yarn rimraf dist",
    "config:build": "yarn tsc --module commonjs  --target es2016 --moduleResolution node --outDir ./dist ./config/*.ts",
    "rel:build": "yarn tsc -p ./config/tsconfig.rel.json",
    "test": "yarn clean && yarn config:build && yarn test:build && yarn packagejson:copy && yarn test:run",
    "test:run": "yarn jest -c ./dist/jest.config.js",
    "test:build": "./script/testbuild.sh"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^18.14.2",
    "http-server": "^14.1.1",
    "jest": "^29.4.3",
    "neovim": "^4.10.1",
    "rimraf": "^4.1.3",
    "typescript": "^4.9.5"
  },
  "dependencies": {
  }
}
